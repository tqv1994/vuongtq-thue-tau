The Node autocomplete edit link module will add an edit link next to any
node reference autocomplete field that is populated, linking to the
edit form for the node


Install
-------
Installing the module is simple:

1) Copy the nodereference_filter folder to the modules folder in your
   installation, this is a child module.

2) Enable the module using Administer -> Modules (#overlay=admin/modules)
